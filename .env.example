# Copy to .env and adjust as needed. .env is git-ignored.
# The Firebase web config is hardcoded in src/lib/firebase.js (public web keys),
# so this file is optional today — it exists for the API base URL.

# Base URL of the game API (separate repo: xtocodex/sena-missions-api).
# Only needed if/when the dashboard calls the API over HTTP.
# Read it in code via import.meta.env.VITE_API_BASE_URL
VITE_API_BASE_URL=https://sena-missions-api-332405485338.us-central1.run.app
